name: Test, Lint and Build

on:
    push:
    
jobs:
    build:
        runs-on: ubuntu-24.04
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            
            - name: Node
              uses: actions/setup-node@v4
              with:
                node-version: 22
                cache: 'npm'

            - name: Get packages
              run: npm ci

            - name: Build
              run: npm run build

            - name: Lint
              run: npm run lint

            # - name: Test
            #   run: npm test
            
