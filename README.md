# dreams-UI

This UI runs together with [dreams-api](https://github.com/bbrighter/dreams-api).

# Configuration
You can adapt the API URL via `public/config.json` with content like 
```json
{
    "apiUrl": "http://localhost:5000"
}
```